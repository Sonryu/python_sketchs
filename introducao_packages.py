#so pra fazer o commit msm, to estudando por livrokkkkkkkkk

from sys import path

import package_aula.package

from package_aula.package import soma

from package_aula import package

from package_aula.package import *




print (soma(1, 2))
print (package_aula.package.soma(1, 2)) 
print (package.soma(1, 2))
print (var)
print (nova_var)